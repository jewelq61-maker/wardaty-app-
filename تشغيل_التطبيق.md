# 🚀 دليل تشغيل تطبيق Wardaty على الكمبيوتر

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت البرامج التالية على جهازك:

### 1. Node.js (إصدار 18 أو أحدث)
- تحميل من: https://nodejs.org/
- تحقق من التثبيت:
  ```bash
  node --version
  npm --version
  ```

### 2. Git
- تحميل من: https://git-scm.com/
- تحقق من التثبيت:
  ```bash
  git --version
  ```

### 3. لتشغيل التطبيق على الموبايل:
- **لأجهزة Android**: تثبيت تطبيق **Expo Go** من Google Play Store
- **لأجهزة iOS**: تثبيت تطبيق **Expo Go** من App Store

### 4. لتشغيل التطبيق على المحاكي (اختياري):
- **Android**: تثبيت Android Studio و Android Emulator
- **iOS** (فقط على Mac): تثبيت Xcode و iOS Simulator

---

## 📥 الخطوة 1: تحميل المشروع

### طريقة 1: استنساخ المشروع من GitHub

```bash
# انسخ المشروع إلى جهازك
git clone https://github.com/jewelq61-maker/wardaty-app-.git

# ادخل إلى مجلد المشروع
cd wardaty-app-
```

### طريقة 2: تحميل ZIP
1. اذهب إلى: https://github.com/jewelq61-maker/wardaty-app-
2. انقر على زر "Code" الأخضر
3. اختر "Download ZIP"
4. فك الضغط وافتح المجلد في Terminal/CMD

---

## 📦 الخطوة 2: تثبيت المكتبات

افتح Terminal/CMD في مجلد المشروع ونفذ الأمر التالي:

```bash
npm install
```

⏱️ **ملاحظة**: قد يستغرق هذا من 2-5 دقائق حسب سرعة الإنترنت.

---

## 🎯 الخطوة 3: تشغيل التطبيق

### طريقة 1: تشغيل على Expo Go (الأسهل - موصى به للمبتدئين)

```bash
npx expo start
```

بعد تشغيل الأمر، سيظهر لك:
- **QR Code** في Terminal
- **صفحة ويب** في المتصفح على `http://localhost:8081`

**لتشغيل التطبيق على الموبايل:**
1. افتح تطبيق **Expo Go** على هاتفك
2. امسح الـ QR Code بكاميرا التطبيق
3. انتظر قليلاً... سيفتح التطبيق تلقائياً! ✨

**لتشغيل التطبيق على المحاكي:**
- اضغط `a` لفتح Android Emulator
- اضغط `i` لفتح iOS Simulator (Mac فقط)

### طريقة 2: تشغيل مع Server الباك إند (للمطورين)

```bash
npm run all:dev
```

هذا الأمر سيشغل:
- ✅ تطبيق Expo (Frontend)
- ✅ Server Node.js (Backend)

---

## 🛠️ أوامر إضافية مفيدة

### تشغيل Server فقط
```bash
npm run server:dev
```

### تشغيل Expo فقط
```bash
npm run expo:dev
```

### فحص الأخطاء (Linting)
```bash
npm run lint
```

### إصلاح الأخطاء تلقائياً
```bash
npm run lint:fix
```

### فحص أنواع TypeScript
```bash
npm run check:types
```

### تنسيق الكود
```bash
npm run format
```

---

## 📱 خيارات التشغيل المتاحة

بعد تشغيل `npx expo start`، يمكنك الضغط على:

| المفتاح | الوظيفة |
|---------|---------|
| `a` | فتح على Android Emulator |
| `i` | فتح على iOS Simulator |
| `w` | فتح على المتصفح (Web) |
| `r` | إعادة تحميل التطبيق |
| `m` | عرض القائمة |
| `j` | فتح أدوات المطور |
| `c` | مسح الـ cache |
| `?` | عرض جميع الخيارات |

---

## 🏗️ بناء التطبيق (Build)

### للحصول على ملف APK/IPA للتثبيت على الجهاز:

#### 1. تثبيت EAS CLI
```bash
npm install -g eas-cli
```

#### 2. تسجيل الدخول
```bash
npx eas-cli login
```
أدخل بريدك الإلكتروني وكلمة المرور لحساب Expo.

#### 3. بناء التطبيق

**لأجهزة Android (APK):**
```bash
npx eas-cli build --platform android --profile preview
```

**لأجهزة iOS:**
```bash
npx eas-cli build --platform ios --profile preview
```

**للمنصتين معاً:**
```bash
npx eas-cli build --platform all --profile preview
```

#### 4. متابعة وتحميل Build
بعد تشغيل الأمر:
1. سيظهر رابط لمتابعة Build على Expo Dashboard
2. انتظر 5-10 دقائق حتى ينتهي Build
3. حمّل APK أو IPA من Dashboard
4. ثبّت التطبيق على جهازك

---

## 🐛 حل المشاكل الشائعة

### مشكلة: "Cannot find module" أو "Module not found"
**الحل:**
```bash
# امسح المجلدات وأعد التثبيت
rm -rf node_modules
rm package-lock.json
npm install
```

### مشكلة: "Port 8081 already in use"
**الحل:**
```bash
# أغلق العملية على Port 8081
# Windows:
netstat -ano | findstr :8081
taskkill /PID <PID_NUMBER> /F

# Mac/Linux:
lsof -ti:8081 | xargs kill -9

# أو استخدم port آخر:
npx expo start --port 8082
```

### مشكلة: لا يظهر QR Code
**الحل:**
```bash
# شغل بدون tunnel
npx expo start --localhost
```

### مشكلة: Expo Go لا يتصل
**الحل:**
- تأكد أن الموبايل والكمبيوتر على نفس شبكة الواي فاي
- أطفئ Firewall مؤقتاً
- جرب استخدام tunnel:
  ```bash
  npx expo start --tunnel
  ```

### مشكلة: أخطاء TypeScript
**الحل:**
```bash
# تجاهل أخطاء TypeScript مؤقتاً
export NODE_OPTIONS=--max-old-space-size=4096
npx expo start --clear
```

---

## 📂 هيكل المشروع

```
wardaty-app-/
├── App.js                    # نقطة البداية الرئيسية
├── app.json                  # إعدادات التطبيق
├── package.json              # المكتبات والأوامر
│
├── components/               # المكونات القابلة لإعادة الاستخدام
│   ├── Card.tsx             # ✅ تم إصلاح خطأ borderWidth
│   ├── UIKit.tsx            # ✅ تم إصلاح خطأ borderWidth
│   └── ...
│
├── screens/                  # شاشات التطبيق
│   ├── HomeScreen.tsx
│   ├── ProfileScreen.tsx
│   └── ...
│
├── constants/                # الثوابت والإعدادات
│   ├── theme.ts             # نظام الألوان والتصميم
│   └── ...
│
├── navigation/               # التنقل بين الشاشات
├── hooks/                    # Custom React Hooks
├── contexts/                 # React Contexts
├── lib/                      # مكتبات مساعدة
├── server/                   # Backend API
└── assets/                   # الصور والخطوط
```

---

## ✅ الإصلاحات الأخيرة

### تم إصلاح خطأ borderWidth (اليوم)
**المشكلة:**
```
ERROR [runtime not ready]: TypeError: Cannot read property 'borderWidth' of undefined
```

**الحل:**
- ✅ تم تصحيح استيراد `GlassEffect` إلى `GlassEffects` في Card.tsx
- ✅ تم تصحيح استيراد `GlassEffect` إلى `GlassEffects` في UIKit.tsx
- ✅ تم تحديث جميع الاستخدامات من `GlassEffect.borderWidth` إلى `GlassEffects.light.borderWidth`

**الملفات المعدلة:**
- `components/Card.tsx`
- `components/UIKit.tsx`

---

## 🎓 تعلم المزيد

- **Expo Docs**: https://docs.expo.dev/
- **React Native Docs**: https://reactnative.dev/
- **TypeScript Docs**: https://www.typescriptlang.org/

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم "حل المشاكل الشائعة" أعلاه
2. ابحث في Issues على GitHub: https://github.com/jewelq61-maker/wardaty-app-/issues
3. اطلب المساعدة في Expo Forums: https://forums.expo.dev/

---

## 🎉 ملخص سريع

**للبدء السريع:**
```bash
# 1. استنسخ المشروع
git clone https://github.com/jewelq61-maker/wardaty-app-.git
cd wardaty-app-

# 2. ثبت المكتبات
npm install

# 3. شغل التطبيق
npx expo start

# 4. امسح QR Code بتطبيق Expo Go على موبايلك
```

**وبس! 🚀**

---

**آخر تحديث:** 11 ديسمبر 2025  
**الحالة:** ✅ جاهز للتشغيل  
**الإصدار:** 1.0.0
